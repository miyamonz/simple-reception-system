version: '3'
services:
    cgi:
        build: 
            context: ./docker
        ports:
            - 8000:8080
        tty: true
        volumes:
            - ./docker/html/:/app/html/
        env_file:
            - .env
